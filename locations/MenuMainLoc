# MenuMainLoc

! WD: broken 'Menu' fix, ARGS[0] is set to menu position
if ARGS[0] = 1:
	ETOmenu = 0
	gs 'stat'
end

if ARGS[0] = 2:
	ETOmenu = 1
	gs 'stat'
end

! WD: Original code
if $ARGS[0] = '0':
	ETOmenu = 0
	gs 'stat'
end

if $ARGS[0] = '1':
	ETOmenu = 1
	gs 'stat'
end

--- MenuMainLoc ---------------------------------

